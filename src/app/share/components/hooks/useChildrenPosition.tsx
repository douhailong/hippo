import React from 'react';

type UseChildrenPositionProps = {
  gap: string;
  col: string;
};
const useChildrenPosition = ({}: UseChildrenPositionProps) => {
  return <div>useChildrenPosition</div>;
};

export default useChildrenPosition;
